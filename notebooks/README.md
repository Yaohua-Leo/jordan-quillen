# Notebooks

Notebook files are exploratory. Keep durable conclusions in `theory/`,
`literature/notes/`, or `experiments/*/results.*`.

Current notebooks are empty placeholders so the project map is stable while the
compute backend remains undecided.
