# Experiments

Experiments are planned computations, not claims.

Rules:

- Every experiment needs an `experiment.md` with purpose, inputs, and status.
- Results may say `not run yet`.
- Do not treat an experiment result as a theorem until it is linked to a claim
  and reviewed.
